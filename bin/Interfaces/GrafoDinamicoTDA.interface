package interfaces;

public interface GrafoListaTDA {
    void inicializarGrafo();
    void agregarVertice(int vertice);
    void agregarArista(int origen, int destino, int peso);
    boolean existeVertice(int vertice);
    boolean existeArista(int origen, int destino);
    int pesoArista(int origen, int destino);
    int[] vertices();
    int[] adyacentes(int vertice);
}

